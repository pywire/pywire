---
title: Event modifiers
tutorial: Basic PyWire
section: Event handlers
description: Use modifiers to change event behavior, like @click.once.
files:
  - path: pages/index.wire
    initial: |
      ---
      count = wire(0)
      triggered = wire("")
      ---
      <div>
        <h1>Modifiers</h1>
        <p>Count: {count}</p>
        <p style="color: green; font-weight: bold;">{triggered}</p>
        
        <div class="stack">
          <button @click={count.value += 1}>
            Click Many Times
          </button>

          <button>
            Click Only Once
          </button>
        </div>
      </div>
    solution: |
      ---
      count = wire(0)
      triggered = wire("")
      ---
      <div>
        <h1>Modifiers</h1>
        <p>Count: {count}</p>
        <p style="color: green; font-weight: bold;">{triggered}</p>
        
        <div class="stack">
          <button @click={count.value += 1}>
            Click Many Times
          </button>

          <button @click.once={triggered.value = "✓ Triggered once!"}>
            Click Only Once
          </button>
        </div>
      </div>
successCriteria:
  - type: file_contains
    description: 'Use @click.once on the second button'
    target: pages/index.wire
    pattern: '@click.once'
  - type: browser_route_text
    route: '/'
    description: "Click the 'Once' button to see the success message"
    pattern: 'Triggered'
pagesDir: pages
---

PyWire supports event modifiers to handle common patterns without writing extra boilerplate. Modifiers are added after the event name with a dot.

### Your Task

1.  Add `@click.once={triggered.value = "✓ Triggered once!"}` to the "Click Only Once" button.
2.  Click that button multiple times and notice that the success message only appears once, no matter how many times you click.

Other common modifiers include `.prevent` (to prevent default), `.stop` (to stop propagation), and `.enter` (for keyup events).
