---
title: Dynamic attributes
tutorial: Basic PyWire
section: Introduction
description: Bind attributes to Python expressions using curly braces.
files:
  - path: pages/index.wire
    initial: |
      src = "https://raw.githubusercontent.com/pywire/pywire/main/assets/logo.png"
      alt = "PyWire Logo"

      ---html---
      <div>
        <h1>Dynamic Attributes</h1>
        <img src="" alt="" />
      </div>
    solution: |
      src = "https://raw.githubusercontent.com/pywire/pywire/main/assets/logo.png"
      alt = "PyWire Logo"

      ---html---
      <div>
        <h1>Dynamic Attributes</h1>
        <img {src} {alt} />
      </div>
successCriteria:
  - type: file_contains
    description: "Use {src} shorthand for the image source"
    target: pages/index.wire
    pattern: "{src}"
  - type: browser_element
    description: "Ensure the image has the correct alt text"
    target: "img[alt='PyWire Logo']"
pagesDir: pages
---

Just like content interpolation, you can use curly braces to make HTML attributes dynamic.

### Your Task

1.  Update the `<img>` tag to use the `src` and `alt` variables. You can use the shorthand syntax: `<img {src} {alt} />`.
2.  Verify that the PyWire logo appears in the preview.

If the attribute name and the variable name are the same, `{src}` is equivalent to `src={src}`.
