---
title: The Counter
description: Now let's add some state. Use wire() and $count syntax.
initialCode: |
  !title "Counter"

  count = wire(0)

  def increment():
      $count += 1

  ---html---
  <div>
    <p>Count: {count.value}</p>
    <button @click={increment}>Increment</button>
  </div>
files: ["index.wire"]
---

## 2. The Counter

Now let's add some state. PyWire uses the `wire()` function to create reactive state, and the `$` prefix to access or mutate that state within Python functions.

### Your Task

Try incrementing the counter!
