!path "/login"
!layout "public-layout.wire"

<section class="brand-hero">
    <span class="brand-eyebrow">Sign in</span>
    <h1>Login</h1>
    <p class="brand-lede">Replace this placeholder with <code>pywire-auth</code> when wiring up your provider.</p>
</section>
