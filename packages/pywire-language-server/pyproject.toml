[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pywire-language-server"
version = "0.7.1"
authors = [
    { name="Reece Holmdahl", email="reece@pywire.dev" },
]
description = "Language Server Protocol implementation for PyWire"
requires-python = ">=3.11"
dependencies = [
    "pygls>=1.3.0",
    "ty>=0.0.15",
    "pywire-parser>=0.8.0",
]

[project.scripts]
pywire-lsp = "pywire_language_server.server:start"

[project.optional-dependencies]
dev = [
    "pytest>=7.4.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.1.0",
    "coverage>=7.3.0",
    "nox",
    "ruff>=0.1.0",
    "ty",
]

[tool.ty.environment]
python-version = "3.11"

[[tool.ty.overrides]]
include = ["tests/**"]
rules = { unresolved-attribute = "ignore", invalid-assignment = "ignore", invalid-argument-type = "ignore", unsupported-operator = "ignore" }

[tool.pytest.ini_options]
asyncio_mode = "auto"

[tool.hatch.build.targets.wheel]
packages = ["src/pywire_language_server"]

