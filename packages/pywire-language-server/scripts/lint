#!/bin/sh -e

export SOURCE_FILES="src/pywire_language_server"

set -x

uv run --package pywire-language-server --extra dev ruff format $SOURCE_FILES
uv run --package pywire-language-server --extra dev ruff check --fix $SOURCE_FILES
uv run --package pywire-language-server --extra dev ty check $SOURCE_FILES
