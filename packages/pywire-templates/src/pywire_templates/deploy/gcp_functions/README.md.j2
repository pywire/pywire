# {{ project_name }} on Google Cloud Functions

Deploy with `gcloud functions deploy {{ project_name }} --gen2 --runtime=python311 --entry-point=pywire --source=. --trigger-http --allow-unauthenticated`.
