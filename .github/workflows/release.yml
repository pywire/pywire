name: Release

on:
  push:
    branches: [main]

permissions:
  contents: write
  pull-requests: write
  deployments: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    outputs:
      releases_created: ${{ steps.release.outputs.releases_created }}
      pywire--release_created: ${{ steps.release.outputs['packages/pywire--release_created'] }}
      pywire--tag_name: ${{ steps.release.outputs['packages/pywire--tag_name'] }}
      pywire-language-server--release_created: ${{ steps.release.outputs['packages/pywire-language-server--release_created'] }}
      pywire-language-server--tag_name: ${{ steps.release.outputs['packages/pywire-language-server--tag_name'] }}
      vscode-pywire--release_created: ${{ steps.release.outputs['packages/vscode-pywire--release_created'] }}
      vscode-pywire--tag_name: ${{ steps.release.outputs['packages/vscode-pywire--tag_name'] }}
      tree-sitter-pywire--release_created: ${{ steps.release.outputs['packages/tree-sitter-pywire--release_created'] }}
      prettier-plugin-pywire--release_created: ${{ steps.release.outputs['packages/prettier-plugin-pywire--release_created'] }}
      create-pywire-app--release_created: ${{ steps.release.outputs['packages/create-pywire-app--release_created'] }}
      pywire-docs--release_created: ${{ steps.release.outputs['docs--release_created'] }}
    steps:
      - uses: googleapis/release-please-action@v4
        id: release
        with:
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json

      # release-please PRs are created with GITHUB_TOKEN, which suppresses
      # pull_request workflow triggers. Set success statuses for all required
      # CI checks so branch protection doesn't block merge.
      - name: Set CI status on release-please PRs
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          prs=$(gh pr list \
            --repo "${{ github.repository }}" \
            --json number,headRefOid,headRefName \
            --jq '[.[] | select(.headRefName | startswith("release-please--"))]')

          if [ "$prs" = "[]" ] || [ -z "$prs" ]; then
            echo "No open release-please PRs"
            exit 0
          fi

          checks=(
            "Check PyWire Core"
            "Check Language Server"
            "Check VS Code Extension"
            "Check Prettier Plugin"
            "Check Tree-sitter Grammar"
            "Check Create PyWire App"
            "Check Examples"
            "Check PyWire Docs"
            "Python Tests"
          )

          echo "$prs" | jq -c '.[]' | while read -r pr; do
            sha=$(echo "$pr" | jq -r '.headRefOid')
            num=$(echo "$pr" | jq -r '.number')
            echo "Setting CI statuses on PR #$num ($sha)"
            for context in "${checks[@]}"; do
              gh api "repos/${{ github.repository }}/statuses/$sha" \
                -f state=success \
                -f context="$context" \
                -f description="Skipped for release-please PR" \
                --silent
            done
          done

      - name: Auto-approve and auto-merge release-please PRs
        env:
          GH_TOKEN: ${{ secrets.RELEASE_PAT }}
        run: |
          prs=$(gh pr list \
            --repo "${{ github.repository }}" \
            --json number,headRefName \
            --jq '[.[] | select(.headRefName | startswith("release-please--"))]')

          if [ "$prs" = "[]" ] || [ -z "$prs" ]; then
            echo "No open release-please PRs"
            exit 0
          fi

          echo "$prs" | jq -c '.[]' | while read -r pr; do
            num=$(echo "$pr" | jq -r '.number')
            echo "Approving and enabling auto-merge on PR #$num"
            gh pr review "$num" --approve --repo "$GITHUB_REPOSITORY" || true
            gh pr merge "$num" --auto --squash --repo "$GITHUB_REPOSITORY" || true
          done

  # Build pywire wheels across all platforms/architectures
  build-pywire-wheels:
    name: "Build pywire wheels"
    needs: release-please
    if: needs.release-please.outputs.pywire--release_created == 'true'
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64
            manylinux: auto
          - os: ubuntu-latest
            target: aarch64
            manylinux: auto
          - os: macos-latest
            target: universal2-apple-darwin
          - os: windows-latest
            target: x86_64
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - name: Build JS client
        run: pnpm install && pnpm build
        working-directory: packages/pywire/src/pywire/client
      - uses: PyO3/maturin-action@v1
        with:
          working-directory: packages/pywire
          command: build
          target: ${{ matrix.target }}
          manylinux: ${{ matrix.manylinux || '' }}
          args: --release --out dist --find-interpreter
      - uses: actions/upload-artifact@v4
        with:
          name: pywire-wheels-${{ matrix.os }}-${{ matrix.target }}
          path: packages/pywire/dist/*.whl

  # Build pywire source distribution
  build-pywire-sdist:
    name: "Build pywire sdist"
    needs: release-please
    if: needs.release-please.outputs.pywire--release_created == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - name: Build JS client
        run: pnpm install && pnpm build
        working-directory: packages/pywire/src/pywire/client
      - uses: PyO3/maturin-action@v1
        with:
          working-directory: packages/pywire
          command: sdist
          args: --out dist
      - uses: actions/upload-artifact@v4
        with:
          name: pywire-sdist
          path: packages/pywire/dist/*.tar.gz

  # Publish pywire to PyPI
  publish-pywire:
    name: "Publish pywire to PyPI"
    needs: [build-pywire-wheels, build-pywire-sdist]
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: pywire-*
          path: dist
          merge-multiple: true
      - uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/

  # Publish language server to PyPI
  publish-language-server:
    name: "Publish pywire-language-server to PyPI"
    needs: release-please
    if: needs.release-please.outputs.pywire-language-server--release_created == 'true'
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - run: uv build --out-dir dist
        working-directory: packages/pywire-language-server
      - uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: packages/pywire-language-server/dist/

  # Publish VS Code extension
  publish-vscode:
    name: "Publish vscode-pywire"
    needs: release-please
    if: needs.release-please.outputs.vscode-pywire--release_created == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - run: |
          cd packages/vscode-pywire
          pnpm install
          pnpm run package
          npx vsce publish --no-dependencies -p ${{ secrets.VSCE_TOKEN }}
          npx ovsx publish --no-dependencies -p ${{ secrets.OVSX_TOKEN }}

  # Publish create-pywire-app to PyPI
  publish-create-app:
    name: "Publish create-pywire-app to PyPI"
    needs: release-please
    if: needs.release-please.outputs.create-pywire-app--release_created == 'true'
    runs-on: ubuntu-latest
    permissions:
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - run: uv build --out-dir dist
        working-directory: packages/create-pywire-app
      - uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: packages/create-pywire-app/dist/

  # Check if docs/ changed since the previous pywire release (only runs on pywire release)
  check-docs-changed:
    name: "Check if docs changed since last pywire release"
    needs: release-please
    if: needs.release-please.outputs.pywire--release_created == 'true'
    runs-on: ubuntu-latest
    outputs:
      changed: ${{ steps.check.outputs.changed }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - id: check
        run: |
          PREV_TAG=$(git tag --sort=-version:refname | grep '^pywire-v' | sed -n '2p')
          if [ -z "$PREV_TAG" ] || ! git diff --quiet "$PREV_TAG"..HEAD -- docs/; then
            echo "changed=true" >> $GITHUB_OUTPUT
          else
            echo "changed=false" >> $GITHUB_OUTPUT
          fi

  # Merge the docs release PR when pywire releases and docs changed (keeps manifest in sync)
  merge-docs-release-pr:
    name: "Merge docs release PR"
    needs: [release-please, check-docs-changed]
    if: >-
      needs.release-please.outputs.pywire--release_created == 'true'
      && needs.check-docs-changed.result == 'success'
      && needs.check-docs-changed.outputs.changed == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Merge docs release PR
        env:
          GH_TOKEN: ${{ secrets.RELEASE_PAT }}
        run: |
          gh pr merge release-please--branches--main--components--pywire-docs \
            --squash \
            --repo "$GITHUB_REPOSITORY" \
            || true

  # Deploy docs to Cloudflare Pages (on pywire release with doc changes, or explicit docs release)
  deploy-docs:
    name: "Deploy Docs (Production)"
    needs: [release-please, check-docs-changed]
    if: >-
      always()
      && needs.release-please.result == 'success'
      && (
        (needs.release-please.outputs.pywire--release_created == 'true'
         && needs.check-docs-changed.result == 'success'
         && needs.check-docs-changed.outputs.changed == 'true')
        || needs.release-please.outputs.pywire-docs--release_created == 'true'
      )
    uses: ./.github/workflows/deploy-docs.yml
    with:
      environment: production
    secrets:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
