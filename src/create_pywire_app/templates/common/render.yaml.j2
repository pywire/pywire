services:
  - type: web
    name: {{ project_name }}
    runtime: python
    buildCommand: pip install uv && uv sync --frozen
    startCommand: uv run pywire run --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
